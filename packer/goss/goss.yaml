package:
  docker-ce:
    installed: true
    versions:
    - {{ .Vars.docker_version }}
  kubeadm:
    installed: true
    versions:
    - {{ .Vars.k8s_version }}
  kubectl:
    installed: true
    versions:
    - {{ .Vars.k8s_version }}
  kubelet:
    installed: true
    versions:
    - {{ .Vars.k8s_version }}
mount:
  swap:
    exists: false
